import { NocharacterDirective } from './nocharacter.directive';

describe('NocharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new NocharacterDirective();
    expect(directive).toBeTruthy();
  });
});
