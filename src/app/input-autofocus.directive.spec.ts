import { InputAutofocusDirective } from './input-autofocus.directive';

describe('InputAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
